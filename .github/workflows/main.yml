name: Upload Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.ASIAVA7OGS2GQYXSPU3Y }}
          aws-secret-access-key: ${{ secrets.v6hmDcEjbWewRIv20CvE4OUdxYKU/D3m+xPZRI1y }}
          # Uncomment the next line if you added AWS_SESSION_TOKEN
          # aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-east-1  # <-- change to your bucket's region

      - name: Deploy static site to S3 bucket
        run: aws s3 sync public/ s3://my-static-site-bucket-sf --delete --exclude ".git/*"
